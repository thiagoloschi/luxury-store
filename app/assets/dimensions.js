const defaultStep = 8;

const dimensions = {
  defaultSpace: `${defaultStep * 2}px`,
  halfSpace: `${defaultStep}px`,
  topBarHeight: `${defaultStep * 8}px`,
  productCardHeight: `${defaultStep * 72}px`,
  productCardWidth: `${defaultStep * 38}px`,
};

export default dimensions;
